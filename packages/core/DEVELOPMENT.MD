# Local Development

### Make sure do you have
- `node` >= 16
- `npm` >= 7

## Installation

```shell
npm install

# Start dev server and build --watch
npm start

# Only dev server
npm run server

# Only build
npm run build
```
