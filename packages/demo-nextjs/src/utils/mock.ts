//#region Project
import { CollectionTypes } from "@/lib/db/models";

export const projectId = 1;

//#region Header & Footer

export const headerId = { collection: CollectionTypes.header, item: "home" };
export const footerId = { collection: CollectionTypes.footer, item: "home" };

//#endregion
